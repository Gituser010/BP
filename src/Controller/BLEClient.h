#include "BLE_Controller.h"

/*

#ifndef BLE_CLIENT_H
#define BLE_CLIENT_H

class BLEClient : public BLEController {
  private:
  void scanSetUp();
public:
  BLEClient(String name);
  BLEScanResults * scan();
  BLEAddress * scanFor(String name);
  bool connect(BLEAddress* pServerAddress);
  bool disconnect();
};

#endif

*/